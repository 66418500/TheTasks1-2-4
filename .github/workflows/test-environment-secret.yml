on:
  push:
    branches:
      - "master"
  deploy:
    needs: test
    runs-on: ubuntu
    defaults:
    steps:
      - name: Extract branch name
        shell: bash
        run: echo ${{ secrets.DOCKERHUB_USERNAME }}
